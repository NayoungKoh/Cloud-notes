

---

## 1. 네트워크 계층

### VPC (Virtual Private Cloud)

- AWS 내 나만의 격리된 가상 네트워크
- CIDR 블록으로 IP 대역 설정 (예: `10.0.0.0/16`)
- 모든 리소스의 기반이 되는 최상위 네트워크 단위

**콘솔:** VPC → VPC 생성 → IPv4 CIDR 입력

---

### 서브넷 (Subnet)

|구분|설명|예시 CIDR|
|---|---|---|
|**퍼블릭 서브넷**|인터넷 게이트웨이와 연결, 외부 접근 가능|`10.0.1.0/24`|
|**프라이빗 서브넷**|외부 직접 접근 불가, 내부 서버/DB 배치|`10.0.2.0/24`|

> 웹서버·DB·WAS는 프라이빗에, 로드밸런서(LB)는 퍼블릭에 배치

**콘솔:** VPC → 서브넷 → 서브넷 생성 → VPC 선택 → 가용영역·CIDR 설정

---

### 게이트웨이

#### 인터넷 게이트웨이 (IGW)

- VPC ↔ 인터넷 간 통신 담당
- 퍼블릭 서브넷의 트래픽을 인터넷으로 내보냄
- VPC에 **1개만** 연결 가능

**콘솔:** VPC → 인터넷 게이트웨이 → 생성 후 → **VPC에 연결(Attach)**

#### NAT 게이트웨이 (NAT-GW)

- 프라이빗 서브넷 → 인터넷 **단방향** 통신 허용 (외부에서 내부 접근은 불가)
- 퍼블릭 서브넷에 위치, **탄력적 IP 필요**
- 소프트웨어 업데이트 등 아웃바운드 트래픽에 사용

**콘솔:** VPC → NAT 게이트웨이 → 퍼블릭 서브넷 선택 → 탄력적 IP 할당

> **실습 변형:** NAT 게이트웨이 대신 IGW를 프라이빗 서브넷 라우팅에 직접 연결하는 구성도 실습

---

### 라우팅 테이블 (Route Table)

- 서브넷의 트래픽 경로를 정의하는 규칙 테이블
- 각 서브넷은 라우팅 테이블에 **명시적으로 연결**해야 함

|라우팅 테이블|대상|타깃|
|---|---|---|
|퍼블릭 RT|`0.0.0.0/0`|IGW|
|프라이빗 RT|`0.0.0.0/0`|NAT-GW|

**콘솔:** VPC → 라우팅 테이블 → 라우팅 편집 → 서브넷 연결 탭에서 서브넷 지정

---

## 2. 컴퓨팅 계층

### EC2 (Elastic Compute Cloud)

- AWS의 가상 서버 인스턴스
- AMI(이미지)를 기반으로 생성, 서브넷·보안그룹 지정 필수

**콘솔:** EC2 → 인스턴스 시작 → AMI 선택 → 인스턴스 유형 → 네트워크(VPC·서브넷) → 보안그룹 설정

---

### AMI (Amazon Machine Image)

- EC2 인스턴스의 **OS + 설정 스냅샷**
- 동일한 서버를 빠르게 복제할 때 사용
- Auto Scaling의 시작 템플릿에 AMI ID 지정

**콘솔:** 실행 중인 인스턴스 → 작업 → 이미지 및 템플릿 → **이미지 생성**

---

### 탄력적 IP (Elastic IP)

- 고정 퍼블릭 IPv4 주소
- EC2 재시작 시에도 IP 유지 (기본 퍼블릭 IP는 재시작마다 변경됨)
- NAT 게이트웨이에도 필수로 사용

**콘솔:** EC2 → 탄력적 IP → 할당 → 인스턴스/NAT-GW에 **연결(Associate)**

---

## 3. 접근 및 보안

### 보안 그룹 (Security Group)

- EC2·LB 등에 붙는 **인스턴스 단위 방화벽**
- 인바운드/아웃바운드 규칙으로 포트·IP 제어
- Stateful (인바운드 허용 시 응답 자동 허용)

|용도|인바운드 허용 예시|
|---|---|
|웹 서버|80(HTTP), 443(HTTPS) from `0.0.0.0/0`|
|Bastion Host|22(SSH) from 내 IP|
|프라이빗 EC2|22(SSH) from Bastion 보안그룹 ID|

**콘솔:** EC2 → 보안 그룹 → 생성 → 인바운드 규칙 편집

---

### Bastion Host

- 퍼블릭 서브넷에 위치한 **점프 서버**
- 외부에서 직접 접근 불가한 프라이빗 EC2에 SSH 접속하기 위한 중간 경유지

```
[내 맥 터미널]
    ↓ SSH (22)
[Bastion Host - 퍼블릭 서브넷]
    ↓ SSH (22)
[프라이빗 EC2 - 프라이빗 서브넷]
```

**맥 터미널 SSH 접속:**

```bash
# Bastion 접속
ssh -i key.pem ec2-user@<Bastion-퍼블릭-IP>

# Bastion에서 프라이빗 EC2 접속
ssh -i key.pem ec2-user@<프라이빗-EC2-내부-IP>
```

---

## 4. 트래픽 분산 및 확장

### 로드밸런서 (Load Balancer)

- 외부 트래픽을 여러 EC2 인스턴스에 **분산**
- 퍼블릭 서브넷에 배치, 프라이빗 EC2와 연결
- ALB(Application LB, HTTP/HTTPS 기반) 주로 사용

**콘솔:** EC2 → 로드밸런서 → 생성 → ALB 선택 → 퍼블릭 서브넷 선택 → 리스너 설정 → **대상 그룹 연결**

---

### 대상 그룹 (Target Group)

- 로드밸런서가 트래픽을 전달할 **EC2 인스턴스 묶음**
- 헬스 체크(Health Check)로 정상 인스턴스만 유지

**콘솔:** EC2 → 대상 그룹 → 생성 → 인스턴스 유형 → 포트 설정 → 인스턴스 등록

---

### Auto Scaling

- 트래픽에 따라 EC2 수를 **자동으로 증감**
- 최소/최대/희망 인스턴스 수 설정
- CloudWatch 지표(CPU 사용률 등)로 스케일 인/아웃

**구성 요소:**

```
시작 템플릿 (AMI + 인스턴스 유형 + 보안그룹)
    ↓
Auto Scaling 그룹 (VPC·서브넷·대상그룹 연결)
    ↓
조정 정책 (CloudWatch 알람 기반)
```

**콘솔:** EC2 → Auto Scaling 그룹 → 생성 → 시작 템플릿 선택 → 서브넷 선택 → LB 대상그룹 연결 → 조정 정책(CloudWatch) 설정

---

## 5. 스토리지

### S3 (Simple Storage Service)

- 객체 기반 스토리지 (파일, 이미지, 로그 등)
- 버킷(Bucket) 단위로 관리
- 정적 웹사이트 호스팅, EC2 백업, 로그 저장 등에 활용
- 퍼블릭 액세스 차단 설정 주의

**콘솔:** S3 → 버킷 만들기 → 리전·이름 설정 → 퍼블릭 액세스 차단 여부 결정

---

## 6. 전체 구성 관계도

```
인터넷
  │
  ▼
[인터넷 게이트웨이 (IGW)]
  │
  ▼
[퍼블릭 서브넷 10.0.1.0/24]
  ├── 로드밸런서 (ALB)          ← 외부 트래픽 수신
  ├── NAT 게이트웨이            ← 프라이빗 → 인터넷 아웃바운드
  └── Bastion Host (EC2)       ← SSH 점프 서버
          │
          │ (퍼블릭 라우팅 테이블: 0.0.0.0/0 → IGW)
          │
  ▼
[프라이빗 서브넷 10.0.2.0/24]
  └── EC2 (웹서버 / WAS / DB)  ← LB에서 트래픽 분산 수신
          │
          │ (프라이빗 라우팅 테이블: 0.0.0.0/0 → NAT-GW)
          │
  ▼
[NAT 게이트웨이] → 인터넷 (소프트웨어 업데이트 등 아웃바운드)
```

### 리소스 생성 순서

```
1. VPC 생성
2. 서브넷 생성 (퍼블릭 + 프라이빗)
3. 인터넷 게이트웨이 생성 → VPC에 연결
4. 탄력적 IP 할당
5. NAT 게이트웨이 생성 (퍼블릭 서브넷 + 탄력적 IP)
6. 라우팅 테이블 생성 및 서브넷 연결
7. 보안 그룹 생성
8. EC2 생성 (Bastion + 프라이빗 서버)
9. AMI 생성 (Auto Scaling용)
10. 대상 그룹 생성
11. 로드밸런서 생성 → 대상 그룹 연결
12. Auto Scaling 그룹 생성
13. S3 버킷 생성
```

> **삭제 시 : LB → Auto Scaling → NAT-GW → 탄력적 IP 해제 → IGW 분리 → 서브넷 → VPC 순으로 삭제해야 오류 없음